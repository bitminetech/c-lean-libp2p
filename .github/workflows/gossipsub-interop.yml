name: GossipSub Interop

on:
  workflow_dispatch:
    inputs:
      pair:
        description: 'Interop pair to run, or all'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - c-lean-libp2p/go-libp2p
          - c-lean-libp2p/rust-libp2p
          - c-lean-libp2p/go-libp2p/rust-libp2p
      scenario:
        description: 'Scenario to run, or all'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - subnet-blob-msg
          - simple-fanout
          - partial-messages
          - partial-messages-chain
          - partial-messages-fanout
      quic_debug:
        description: 'Set LIBP2P_QUIC_DEBUG=1 for c-lean nodes'
        required: false
        default: '0'
        type: choice
        options:
          - '0'
          - '1'
  schedule:
    - cron: '43 8 * * *'
  pull_request:
    paths:
      - 'CMakeLists.txt'
      - 'src/protocol/gossipsub/**'
      - 'include/protocol/gossipsub/**'
      - 'interop/**'
      - 'external/**'
      - '.github/workflows/gossipsub-interop.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SHADOW_REF: v3.3.0
  SHADOW_IMAGE: c-lean-libp2p-gossipsub-shadow:v3.3.0
  SHADOW_IMAGE_TAR: c-lean-libp2p/.cache/gossipsub-shadow/shadow-image.tar
  GOSSIPSUB_INTEROP_PAIR: ${{ github.event.inputs.pair || 'all' }}
  GOSSIPSUB_INTEROP_SCENARIO: ${{ github.event.inputs.scenario || 'all' }}
  GOSSIPSUB_INTEROP_QUIC_DEBUG: ${{ github.event.inputs.quic_debug || '0' }}

jobs:
  gossipsub-cross-impl:
    name: gossipsub cross-impl
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: c-lean-libp2p
          submodules: recursive

      - name: Restore cached Shadow Docker image
        id: shadow-image-cache
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.SHADOW_IMAGE_TAR }}
          key: gossipsub-shadow-${{ runner.os }}-${{ env.SHADOW_REF }}-${{ hashFiles('c-lean-libp2p/interop/test-plans/Dockerfile.shadow') }}-v1

      - name: Load cached Shadow Docker image
        if: steps.shadow-image-cache.outputs.cache-hit == 'true'
        run: |
          test -s "${SHADOW_IMAGE_TAR}"
          docker load -i "${SHADOW_IMAGE_TAR}"
          docker images "${SHADOW_IMAGE}"

      - name: Free disk space
        if: steps.shadow-image-cache.outputs.cache-hit != 'true'
        run: |
          df -h /
          docker system prune -af
          df -h /

      - name: Build Shadow Docker image
        if: steps.shadow-image-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p "$(dirname "${SHADOW_IMAGE_TAR}")"
          docker build \
            --build-arg SHADOW_REF="${SHADOW_REF}" \
            -t "${SHADOW_IMAGE}" \
            -f c-lean-libp2p/interop/test-plans/Dockerfile.shadow \
            c-lean-libp2p/interop/test-plans
          docker save "${SHADOW_IMAGE}" -o "${SHADOW_IMAGE_TAR}"

      - name: Save Shadow Docker image cache
        if: steps.shadow-image-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ${{ env.SHADOW_IMAGE_TAR }}
          key: ${{ steps.shadow-image-cache.outputs.cache-primary-key }}

      - name: Cache go/rust gossipsub impl artifacts
        uses: actions/cache@v4
        with:
          path: |
            c-lean-libp2p/interop/test-plans/gossipsub-interop/.cache
            c-lean-libp2p/interop/test-plans/gossipsub-interop/go-libp2p/gossipsub-bin
            c-lean-libp2p/interop/test-plans/gossipsub-interop/rust-libp2p/target
          key: >-
            gossipsub-impl-artifacts-${{ hashFiles('c-lean-libp2p/interop/test-plans/gossipsub-interop/Makefile', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/patches/**', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/go-libp2p/go.mod', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/go-libp2p/go.sum', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/go-libp2p/*.go', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/rust-libp2p/Cargo.toml', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/rust-libp2p/Cargo.lock', 'c-lean-libp2p/interop/test-plans/gossipsub-interop/rust-libp2p/src/**/*.rs') }}-v3
          restore-keys: |
            gossipsub-impl-artifacts-

      - name: Run gossipsub interop matrix
        id: matrix
        run: |
          docker run --rm --privileged \
            -v "${PWD}/c-lean-libp2p:/workspace" \
            -w /workspace/interop/test-plans/gossipsub-interop \
            -e CARGO_HOME=/workspace/interop/test-plans/gossipsub-interop/.cache/cargo \
            -e C_LEAN_LIBP2P_ROOT=/workspace \
            -e C_LEAN_LIBP2P_BUILD=/workspace/build-gossipsub-interop \
            -e C_LEAN_LIBP2P_GOSSIPSUB_BIN=/workspace/build-gossipsub-interop/bin/c_lean_libp2p_gossipsub_interop \
            -e GOCACHE=/workspace/interop/test-plans/gossipsub-interop/.cache/go/build \
            -e GOMODCACHE=/workspace/interop/test-plans/gossipsub-interop/.cache/go/pkg/mod \
            -e GOSSIPSUB_INTEROP_PAIR="${GOSSIPSUB_INTEROP_PAIR}" \
            -e GOSSIPSUB_INTEROP_SCENARIO="${GOSSIPSUB_INTEROP_SCENARIO}" \
            -e LIBP2P_QUIC_DEBUG="${GOSSIPSUB_INTEROP_QUIC_DEBUG}" \
            "${SHADOW_IMAGE}" \
            bash -lc 'make c-lean-libp2p && make binaries && uv run c_lean_matrix.py --output-root /workspace/interop/test-plans/gossipsub-interop/c-lean-results --pair "${GOSSIPSUB_INTEROP_PAIR}" --scenario "${GOSSIPSUB_INTEROP_SCENARIO}"'
          echo "result_dir=interop/test-plans/gossipsub-interop/c-lean-results" >> "$GITHUB_OUTPUT"

      - name: Fix interop cache ownership
        if: always()
        run: |
          sudo chown -R "$(id -u):$(id -g)" \
            c-lean-libp2p/interop/test-plans/gossipsub-interop/.cache \
            c-lean-libp2p/interop/test-plans/gossipsub-interop/go-libp2p/gossipsub-bin \
            c-lean-libp2p/interop/test-plans/gossipsub-interop/rust-libp2p/target \
            2>/dev/null || true

      - name: Render matrix summary
        if: always()
        run: |
          matrix="c-lean-libp2p/interop/test-plans/gossipsub-interop/c-lean-results/matrix.md"
          if [ -f "${matrix}" ]; then
            cat "${matrix}" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No gossipsub interop matrix was produced." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gossipsub-interop-results
          path: c-lean-libp2p/interop/test-plans/gossipsub-interop/c-lean-results/
          if-no-files-found: warn
          retention-days: 30

      - name: Upload Shadow node logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gossipsub-interop-shadow-node-logs
          path: c-lean-libp2p/interop/test-plans/gossipsub-interop/c-lean-results/**/hosts/**/*
          if-no-files-found: warn
          retention-days: 30
